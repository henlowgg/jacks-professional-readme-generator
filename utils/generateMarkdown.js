// function generateMarkdown(data) {
//     return `# ${data.title}
  
//   `;
//   }
  
//   module.exports = generateMarkdown;




// for some reason unless i leave this here, the whole code breaks, so... it's staying for now